CURRVAL and NEXTVAL
LEVEL
ROWID
ROWNUM

SELECT STUDENTSEQ.currval FROM DUAL;
INSERT INTO STUDENT VALUES (STUDENTSEQ.nextval, ‘Reshma’, ‘PYTHON’, 8767);


SELECT * FROM EMP WHERE ROWNUM <= 5; 

SELECT ROWID, ename FROM emp WHERE deptno = 30;
