---order by is followed by either ascending or descending order.
SELECT * FROM emp ORDER BY ename;
SELECT * FROM emp ORDER BY ename ASC;
SELECT * FROM emp ORDER BY ename DESC;
SELECT eName FROM emp ORDER BY 1;
